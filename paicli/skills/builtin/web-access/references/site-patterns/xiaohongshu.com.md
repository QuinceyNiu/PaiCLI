# xiaohongshu.com

## 架构判断

小红书反爬较强，公开抓取经常失败，页面内容高度依赖浏览器环境和登录态。

## 成功路径

优先使用 Chrome DevTools MCP。打开页面后等待笔记标题、正文或用户信息出现，用 `take_snapshot` 读取文本；如果内容缺失，滚动并重试。

## 常见失败

- `web_fetch` 无正文或返回校验页：直接切浏览器。
- 需要登录或内容不可见：切 shared 模式。
- 页面要求验证：向用户说明限制，不要尝试绕过验证码。
