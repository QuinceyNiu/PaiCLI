# zhuanlan.zhihu.com

## 架构判断

知乎专栏内容可能首屏可见，但正文、评论或图片存在懒加载。登录态会影响可见内容和弹窗。

## 成功路径

先试 `web_fetch`。如果正文缺失或被登录弹窗遮挡，使用浏览器打开，等待标题和正文节点出现，滚动一次后用 `take_snapshot` 读取完整文本。

## 常见失败

- 弹窗遮挡正文：优先读取 snapshot 中正文文本，不要随意点击登录或关闭按钮。
- 内容截断：滚动后再次 snapshot。
